/*
[1] check if the string is at least 1 character long
[2] check if the string is not longer than 10 characters
*/

function stringLength(str) {
  return str.length;
}
module.exports = stringLength;
